import { AlanButtonColorPipe } from './alan-button-color.pipe';

describe('AlanButtonColorPipe', () => {
  it('create an instance', () => {
    const pipe = new AlanButtonColorPipe();
    expect(pipe).toBeTruthy();
  });
});
